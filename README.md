# BARU 백엔드 API 서버

Express.js 기반의 RESTful API 서버입니다.

## 설치 및 실행

### 1. 의존성 설치
```bash
npm install
```

### 2. 환경 변수 설정
`.env` 파일을 생성하고 RapidAPI 키를 설정하세요:
```env
RAPID_API_KEY=your_rapidapi_key_here
PORT=3000
```

RapidAPI 키는 [https://rapidapi.com](https://rapidapi.com)에서 발급받을 수 있습니다.

### 3. 서버 실행
```bash
# 개발 모드 (nodemon 사용)
npm run dev

# 프로덕션 모드
npm start
```

서버는 기본적으로 `http://localhost:3000`에서 실행됩니다.

## API 엔드포인트

### 사용자 관련
- `GET /api/users` - 모든 사용자 조회
- `GET /api/users/:id` - 특정 사용자 조회
- `POST /api/users` - 사용자 생성
- `PUT /api/users/:id` - 사용자 수정
- `DELETE /api/users/:id` - 사용자 삭제

### 여행 관련
- `GET /api/trips` - 모든 여행 조회
- `GET /api/trips/:id` - 특정 여행 조회
- `POST /api/trips` - 여행 생성
- `GET /api/recommendations` - 여행 추천 (쿼리 파라미터 지원)

### RapidAPI 호텔 위치 검색
- `GET /api/hotels/search` - 호텔 위치 검색
  - 필수 파라미터: `query` (도시명 또는 호텔명, 예: "Osaka", "Seoul")
  - 선택 파라미터: `locale` (기본값: "ko_KR")
  
  예시:
  ```
  GET /api/hotels/search?query=Osaka&locale=ko_KR
  ```

### 기타
- `GET /api/health` - 서버 상태 확인

## 테스트 페이지

브라우저에서 `http://localhost:3000`에 접속하면 API 테스트 페이지를 사용할 수 있습니다.

테스트 페이지에서:
- 빠른 테스트 버튼으로 주요 API를 테스트할 수 있습니다
- 커스텀 요청으로 원하는 엔드포인트를 직접 테스트할 수 있습니다
- **RapidAPI 호텔 위치 검색** 섹션에서 호텔 위치를 쉽게 검색할 수 있습니다
- 응답 결과를 JSON 형식으로 확인할 수 있습니다

### RapidAPI 사용 방법

1. 테스트 페이지의 "🏨 RapidAPI 호텔 위치 검색" 섹션으로 이동
2. 검색어 입력 (예: "Osaka", "Seoul", "Tokyo")
3. 언어 설정 선택 (기본값: 한국어)
4. "🔍 호텔 위치 검색하기" 버튼 클릭
5. 응답 결과에서 호텔 위치 정보 확인

## 프로젝트 구조

```
BARU_back/
├── server.js          # 메인 서버 파일
├── routes/
│   └── api.js        # API 라우트 정의
├── public/
│   └── test.html     # API 테스트 페이지
├── .env              # 환경 변수 (직접 생성 필요)
├── .env.example      # 환경 변수 예시 파일
└── package.json      # 프로젝트 의존성
```

## 개발 참고사항

- 현재는 메모리 기반 데이터 저장소를 사용합니다 (실제 프로덕션에서는 데이터베이스 사용 권장)
- CORS가 활성화되어 있어 프론트엔드에서 쉽게 접근할 수 있습니다
- API 응답은 일관된 형식을 따릅니다: `{success: boolean, data?: any, message?: string, error?: string}`
- RapidAPI는 외부 API이므로 API 키가 필요하며, `.env` 파일에 설정해야 합니다
